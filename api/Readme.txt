Before starting the api ensure that:
     -MongoDB and NodeJs are installed
     -Paths to the bin directories have been appended to your Path variable for both tools

To start the api naviagte to /start_api and execute the appropriate startApi script


note - if you need to setup the api database in the mongoDB then follow the steps provided in LoadingDataToMongoDB.txt
